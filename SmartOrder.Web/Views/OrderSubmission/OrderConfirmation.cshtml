@{
    ViewData["Title"] = "Успешна поръчка";
    string token = ViewBag.Token;
}

<div class="container text-center mt-5">
    <h2 class="text-success">✅ Поръчката беше направена успешно!</h2>
    <p class="mt-3">Благодарим за поръчката. Сервтьор ще я донесе съвсем скоро.</p>

    <a href="@Url.Action("Index", "MenuItem", new { token = token })" class="btn btn-primary mt-3">
        Обратно към менюто
    </a>
</div>

